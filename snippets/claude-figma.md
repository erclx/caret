I want to draw: <component or element name>

Here is the spec:

<paste spec or describe the element: shape, color, size, spacing, etc.>

Write step-by-step Figma instructions to produce this exactly. For each step, specify the exact tool or keyboard shortcut to activate, which panel or field to click, and the exact value to enter. Cover:

1. Frame or group setup (dimensions, corner radius, fill)
2. Each layer or shape in order (type, size, position, fill, stroke)
3. Alignment and spacing rules
4. Export settings

Use exact values from the spec. Do not restate design rationale.
