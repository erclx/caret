Before auditing, grep the files in scope for `;` and `—`. Reading alone misses occurrences. Grep cannot. Then audit every match against `standards/prose.md`. Rewrite each violation thoughtfully. Do not lazily swap punctuation out. A semicolon replaced with a period is not a fix. Read the surrounding context and restructure or rephrase as needed.
